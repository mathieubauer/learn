{% extends "dswAmpliBundle::layout.html.twig" %}
{% block title %}{{ parent() }} - Supprimer un commentaire{% endblock %}
{% block dsw_ampli_jumbotron %}{% endblock %}

{% block dsw_ampli_content %}
<div class="container my-5">

    <h1>Supprimer un commentaire</h1>

    <p>Etes-vous certain de vouloir supprimer ce commentaire ?</p>
    <p><strong>{{ commentaire.content }}</strong></p>
    <p>Cette opération est irréversible.</p>

    {# On met l'id de l'annonce dans la route de l'action du formulaire #}
    <form action="{{ path('commentaire_delete', {'id': commentaire.id}) }}" method="post">
    
    <a href="{{ path('ampli_idee_public', {'id': commentaire.idee.id}) }}" class="btn btn-primary">
    Annuler
    </a>
    
    {# Ici j'ai écrit le bouton de soumission à la main #}
    <input type="submit" value="Supprimer" class="btn btn-danger" />
    
    {# Ceci va générer le champ CSRF #}
    {{ form_rest(form) }}
    </form>
    
</div>
{% endblock %}