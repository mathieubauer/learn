{% extends 'base.html.twig' %}

{% block body %}
    <h1>Record</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ record.id }}</td>
            </tr>
            <tr>
                <th>Points</th>
                <td>{{ record.points }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('eval_record_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('eval_record_edit', { 'id': record.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
