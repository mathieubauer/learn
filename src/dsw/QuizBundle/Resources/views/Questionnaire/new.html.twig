{% extends "dswCoreBundle::base.html.twig" %}

{% block body %}
<div class="container my-5">
   
    <h1>Création de questionnaire</h1>
    <p>Pour commencer, créez le questionnaire, puis les questions afférentes</p>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer le questionnaire" class="btn btn-primary" />
    {{ form_end(form) }}

    <a href="{{ path('quiz_questionnaire_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    
</div>
{% endblock %}
